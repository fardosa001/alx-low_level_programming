#include "main.h"
#include <stdio.h>
/**
 * main - Check the code
 * Description: print alphabet in lowercase
 * Return 0
 */
int main (void)
{
	int n;

	for (n = 97; n <= 122; n++);
	{
		_putchar (n);
	}
		_putchar ('\n');
	return (0);
}

