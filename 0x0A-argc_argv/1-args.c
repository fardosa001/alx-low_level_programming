#include "main.h"
#include <stdio.h>
/**
 * main - prints the number of argument passed into it
 * @argv: argument vector
 * @argc: argument count
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);

}
